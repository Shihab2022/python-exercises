1 . For create virtual environment ---> python -m venv env ( here env is the folder name we can put others name also but best practice is use env )

2 . for active the virtual environment we can drag and drop activate.ps1 or activate ( if this environment is activate then in powershell left side you see the folder name inside the bracket (env ))

for install fastapi uwe can read this documentation -- https://fastapi.tiangolo.com/#installation

3 . in the root file we create main.py file after that we can create server for details read documentation \

4 . for create all the api we can check this url http://127.0.0.1:8000/docs
